VITE_APP_ENV=development
VITE_API_BASE_URL=/api/v1

# Enable OAuth buttons when backend Google/GitHub credentials are configured
# VITE_OAUTH_ENABLED=true

# Optional error tracking (Sentry — public DSN)
# VITE_SENTRY_DSN=
